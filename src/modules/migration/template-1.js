
/**
 * Apply schema changes here.
 * 
 * @param Object dbDriver An instance of Objective SQL Driver.
 */
export async function up(dbDriver) {
}

/**
 * Undo the schema changes made in the up() function here.
 * 
 * @param Object dbDriver An instance of Objective SQL Driver.
 */
export async function down(dbDriver) {
}
